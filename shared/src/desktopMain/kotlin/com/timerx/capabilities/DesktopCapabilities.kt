package com.timerx.capabilities

import com.timerx.platform.PlatformCapabilities

val desktopCapabilities = PlatformCapabilities(
    canSystemDynamicTheme = false,
    canVibrate = false,
    hasAnalytics = false
)
